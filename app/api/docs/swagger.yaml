basePath: /api
definitions:
  main.CounterResponse:
    properties:
      value:
        type: integer
    type: object
  main.LLMRequest:
    properties:
      prompt:
        type: string
    type: object
info:
  contact: {}
  description: A simple API with counter and fake LLM endpoints
  title: Simple Go API
  version: "1.0"
paths:
  /counter/next:
    post:
      consumes:
      - application/json
      description: Returns the next sequential number from memory
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/main.CounterResponse'
      summary: Get next counter value
      tags:
      - counter
  /kill:
    post:
      description: Shuts down the application. mode=graceful (default), panic, exit.
        exit accepts code=N query param.
      parameters:
      - description: 'Stop mode: graceful, panic, exit'
        in: query
        name: mode
        type: string
      - description: Exit code (only for mode=exit, default 1)
        in: query
        name: code
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Application shutting down
      summary: Stops the current instance
      tags:
      - debug
  /llm/chat:
    post:
      consumes:
      - application/json
      description: Takes a prompt and streams response tokens via Server-Sent Events
      parameters:
      - description: Prompt text
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/main.LLMRequest'
      produces:
      - text/event-stream
      responses:
        "200":
          description: Event stream
      summary: Fake LLM endpoint
      tags:
      - llm
swagger: "2.0"
